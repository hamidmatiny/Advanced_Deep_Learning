# Core deep learning framework (used for everything: RNNs, Transformers, etc.)
torch>=2.0.0
torchvision>=0.15.0          # Often used with PyTorch for data utils / examples

# Hugging Face ecosystem (fine-tuning BERT, tokenizers, datasets, etc.)
transformers>=4.35.0
datasets>=2.14.0
huggingface_hub>=0.19.0

# Data handling & numerics
numpy>=1.24.0
pandas>=2.0.0

# Visualization (loss curves, attention heatmaps, etc.)
matplotlib>=3.7.0
seaborn>=0.12.0              # Optional but very common for nicer plots

# Utilities & reproducibility
tqdm>=4.66.0                 # Progress bars
jupyter>=1.0.0               # If running notebooks locally
notebook>=7.0.0

# Optional but very useful for text / generation projects
accelerate>=0.25.0           # Helps with device placement & multi-GPU (even on Colab)
bitsandbytes>=0.43.0         # If you ever experiment with quantization
peft>=0.6.0                  # For efficient fine-tuning (LoRA etc.)

# Development / linting (optional, but good for clean code)
black>=24.0.0
flake8>=7.0.0
